# Loop Themes — cross-task pattern log

<!-- Format: | status | date | project | theme-tag | note | metrics-ref | ·
     one theme per row · rows are marked, never deleted. -->
<!-- status ∈ NEW / PROMOTED:<slug> / DISMISSED:<reason> -->
<!-- SessionStart runs themes_pending.py (P4); 10 or more NEW rows trigger one
     injected nudge line. The theme-assessment skill (on-demand, or at the
     nudge) clusters rows by theme-tag, pulls the referenced metrics records,
     and either promotes a theme to a real resource or dismisses it — the
     status rewrite lands in the same atomic commit as any resulting
     change. -->

| status | date | project | theme-tag | note | metrics-ref |
|---|---|---|---|---|---|
